{
  "compilerOptions": {
    "target": "ES6",                      // 或者 "ES2015"
    "module": "commonjs",                 // 使用 CommonJS 模块规范
    "outDir": "./dist",                   // 编译后的输出目录
    "rootDir": "./src",                   // 源代码所在目录
    "strict": true,                       // 启用所有严格类型检查选项
    "esModuleInterop": true,              // 允许从非 ES6 模块导入默认导出
    "skipLibCheck": true,                 // 跳过声明文件检查
    "forceConsistentCasingInFileNames": true  // 强制文件名一致性
  },
  "include": [
    "src/**/*"
  ],
  "baseUrl": ".",
  "paths": {
    "@/*": ["src/*"]
  },
  "exclude": [
    "node_modules",
    "**/*.spec.ts"
  ]
}
